# Scenario pipeline

The scenario pipeline owns scientific configuration, physical-building asset
plans, and stable model-case semantics. It must run without importing
`paired_validation`. See `documentation/SCENARIO_METHOD.md` for the method and
`documentation/CONFIGURATION_REFERENCE.md` for YAML options.
