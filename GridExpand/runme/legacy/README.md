# Legacy runners

`real_swf_powerflow_runner.py` is the superseded HH-only real-grid wrapper used during early status-quo diagnostics. The active paired pipeline now produces pre, post-flex, and post-no-flex summaries for both network models through `../paired_swf_pipeline_runner.py`.
